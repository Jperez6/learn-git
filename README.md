# learn-git

## bash

ls (list all directorys) <br>
cd (current dr) <br>
mkdir (make directory)<br>

## git

status <br>
add (file name)<br>
commit -m ("message")<br>
push (push code to github)<br>
log (see log history)<br>
